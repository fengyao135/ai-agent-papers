# 学术论文翻译与总结工作流

## 一、图片格式规范

- 使用 HTML 居中标签，**不使用** Markdown 图片语法
- 格式：

  ```html
  <p align="center"><img src="image-X.png" width="XXX" /></p>
  ```

- 宽度选择指南：

  | 图片类型 | 建议宽度 |
  |---------|---------|
  | 主图 / 全景图 | 800 |
  | 中等图 | 700 |
  | 小图 / 流程图 | 600 |
  | 紧凑图 | 400 |

- 图片下方紧跟中文图注，使用 blockquote 格式：

  ```markdown
  > **图X：标题。** 描述内容
  ```

- 图注需包含：图编号、简短标题、内容描述

## 二、翻译文件规范

- **文件名格式**：`论文主题_中文翻译.md`
- **文件头部**包含：原文标题、作者、机构、原文链接
- **章节结构**与原文保持一致
- 所有图片按出现顺序编号：`image-1.png`, `image-2.png`, ...
- 图片文件与 Markdown 文件放在**同一目录**

## 三、总结文件规范

- **文件名格式**：`论文主题_论文总结.md`
- **总结结构**（按需调整）：

  | 序号 | 章节 | 说明 |
  |-----|------|------|
  | 1 | 核心主张 | 一句话概括 + 展开说明 |
  | 2 | 问题诊断 | 论文要解决什么问题 |
  | 3 | 历史/背景 | 演进脉络 |
  | 4 | 主要贡献/框架 | 各组成部分各一小节 |
  | 5 | 模块间交互/系统分析 | 整体架构分析 |
  | 6 | 权衡与取舍 | 设计决策的利弊 |
  | 7 | 未来方向 | 开放问题与潜在改进 |
  | 8 | 结论 | 总结性评价 |

- **写作风格要求**：
  - 大量使用**表格对比**，使信息密集且可扫读
  - 优先使用 **Mermaid 图表**展示流程和架构，避免使用 ASCII 图表
  - 重要结论用 **blockquote** (`>`) 突出
  - **按需引用翻译中的截图**：在总结的对应章节插入原文图片 + 中文图注

- **Mermaid 图表示例**：

  ````markdown
  ```mermaid
  graph LR
      A[模块 A] --> B[模块 B] --> C[模块 C]
  ```
  ````

## 四、图片引用规则

- 总结文件引用翻译中的**同一组图片**（同目录，相同文件名）
- 每张图插入到总结中**内容最相关的章节开头**
- 保持与翻译文件**相同的 HTML 格式和宽度设置**
- 图注可在翻译版基础上适当精简

## 五、文件组织规则

```
papers/
├── planning/                      # 规划与推理
├── memory/                        # 记忆机制
├── tool-use/                      # 工具使用
├── multi-agent/                   # 多智能体
├── evaluation/                    # 评估与基准
├── application/                   # 应用场景
├── survey/                        # 综述
└── <分类>/<论文主题>/
    ├── 论文主题_中文翻译.md    # 翻译
    ├── 论文主题_论文总结.md    # 总结
    ├── image-1.png            # 按出现顺序编号
    ├── image-2.png
    └── ...
```

- **按主题分类**：先选择合适的分类目录，再在其下创建论文文件夹
- 文件夹名使用简短的英文论文主题（如 `ReAct`, `Toolformer`, `AutoGPT` 等）
- 图片文件与两个 Markdown 文件放在同一目录下
- 每个分类目录有独立的 `README.md`，维护该分类下的论文列表
